services:
  - type: web
    name: conteo-backend
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn backend911.wsgi"
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: backend_conteo.settings
